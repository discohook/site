import styled from "styled-components"

export const BaseModalHeader = styled.h4`
  color: ${({ theme }) => theme.header.primary};
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.3px;
  line-height: 20px;
  text-transform: uppercase;

  margin: 20px;
`
